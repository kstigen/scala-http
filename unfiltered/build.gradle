apply plugin: 'scala'
repositories {
   mavenCentral()
}

dependencies {
   compile 'org.scala-lang:scala-library:2.12.0'
}

task run(type: JavaExec, dependsOn: classes) {
   main = 'UnfilteredLauncher'
   classpath sourceSets.main.runtimeClasspath
   classpath configurations.runtime
}
